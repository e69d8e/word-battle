[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[functions]
  # 确保 Prisma 能够在 serverless 环境中工作
  included_files = ["node_modules/.prisma/**", "node_modules/@prisma/client/**"]
